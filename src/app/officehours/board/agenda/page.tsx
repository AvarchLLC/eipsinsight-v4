import { AgendaMaker } from "@/app/(tools)/board/agenda/page";

/** Dedicated route for the Office Hour Agenda Maker under the Office Hours board. */
export default function OfficeHoursAgendaPage() {
  return <AgendaMaker />;
}
